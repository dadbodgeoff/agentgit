{
  "name": "@agentgit/authority-cli",
  "version": "0.1.0",
  "private": false,
  "type": "module",
  "main": "dist/main.js",
  "types": "dist/main.d.ts",
  "bin": {
    "agentgit-authority": "dist/main.js"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "pnpm --filter @agentgit/schemas build && pnpm --filter @agentgit/authority-sdk build && tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "clean": "rm -rf dist tsconfig.tsbuildinfo",
    "test": "pnpm --filter @agentgit/schemas build && pnpm --filter @agentgit/authority-sdk build && vitest run --dir src",
    "start": "node dist/main.js"
  },
  "dependencies": {
    "@agentgit/authority-sdk": "workspace:*"
  }
}
